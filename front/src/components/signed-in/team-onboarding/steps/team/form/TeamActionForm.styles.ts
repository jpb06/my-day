import { makeStyles } from "@material-ui/core";

export const styles = makeStyles({
  centered: {
    display: "flex",
    justifyContent: "center",
    textAlign: "center",
  },
  actionIcon: {
    width: 100,
    height: 100,
  },
});
