import { makeStyles } from "@material-ui/core";

export const styles = makeStyles({
  container: {
    backgroundColor: "whitesmoke",
  },
  smallContainer: {
    width: 40,
    height: 40,
  },
  smallLogo: {
    width: 32,
    height: 32,
  },
});
